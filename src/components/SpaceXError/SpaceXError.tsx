import React from "react";
import { Error } from "./SpaceXError.style";

const SpaceXError: React.FC = () => {
  return (
    <Error>
      <h5>SpaceX Error ):</h5>
    </Error>
  );
};

export default SpaceXError;
